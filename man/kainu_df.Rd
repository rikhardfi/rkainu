% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kainu_df.R
\name{kainu_df}
\alias{kainu_df}
\title{Compute Kainu reference values for a data.frame}
\usage{
kainu_df(
  data,
  age_col = "age",
  height_col = "height",
  sex_col = "sex",
  params = c("FEV1", "FVC", "FEV1FVC"),
  output = c("predicted", "lln", "zscore", "pctpred")
)
}
\arguments{
\item{data}{A data.frame containing age, height, sex, and observed
spirometry columns.}

\item{age_col, height_col, sex_col}{Character strings naming the columns for
age (years), height (cm), and sex (1=male, 2=female).}

\item{params}{Character vector of spirometry parameters to compute. Observed
values are read from columns matching these names (e.g., \code{"FEV1"}).}

\item{output}{Character vector specifying which outputs to add. Any
combination of \code{"predicted"}, \code{"lln"}, \code{"zscore"}, \code{"pctpred"}. Defaults
to all four.}
}
\value{
The input data.frame with added columns named
\verb{pred_<param>}, \verb{lln_<param>}, \verb{z_<param>}, \verb{pctpred_<param>}.
Z-score and percent predicted columns are only added for parameters that
exist as columns in the input data.
}
\description{
Convenience wrapper that adds predicted, LLN, z-score, and/or percent
predicted columns to an existing data.frame.
}
\examples{
df <- data.frame(
  age = c(30, 50, 70),
  height = c(180, 175, 170),
  sex = c(1, 1, 2),
  FEV1 = c(4.5, 3.8, 2.0),
  FVC = c(5.5, 4.9, 2.8)
)
kainu_df(df)

}
\references{
Kainu A, Timonen KL, Toikka J, et al. Reference values of spirometry for
Finnish adults. \emph{Clin Physiol Funct Imaging}. 2016;36(5):346-358.
}
